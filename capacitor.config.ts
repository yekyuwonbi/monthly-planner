import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yekyuwonbi.monthlyplanner',
  appName: 'Monthly Planner',
  webDir: 'dist'
};

export default config;
